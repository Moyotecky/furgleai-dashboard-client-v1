import React from 'react';
import { OverviewView } from '@/modules/dashboard';

export const metadata = {
  title: 'Overview - FurgleAI',
};

export default function DashboardOverviewPage() {
  return <OverviewView />;
}
