import React from 'react';
import { PlaceholderView } from '@/modules/analytics/components/PlaceholderView';

export const metadata = { title: 'Live Pipeline Analytics - FurgleAI' };

export default function AnalyticsPipelinePage() {
  return <PlaceholderView title="LiveScan Pipeline Monitor" />;
}
